/**
 * Process view — variants + cost-weighted process graph.
 * Owner: Livana (frontend lane).
 * Phase: Tier 0. This view leads the Round 2 demo — the hour-8 gate
 * checks this rendering end to end, or Tier-2 work gets dropped.
 *
 * Consumes backend/app/api/process.py via ../api/process.ts.
 */
export default function ProcessView() {
  return <div>ProcessView — TODO: cost-weighted process graph.</div>;
}
