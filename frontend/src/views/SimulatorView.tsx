/**
 * Simulator view — counterfactual "what-if".
 * Owner: Livana (frontend lane).
 * Phase: Tier 2. Hour-20 gate: must run end to end, or everyone moves onto
 * it. Protect this view above all others if time runs short.
 *
 * Consumes backend/app/api/simulate.py via ../api/simulate.ts.
 */
export default function SimulatorView() {
  return <div>SimulatorView — TODO: what-if scenario controls + delta output.</div>;
}
