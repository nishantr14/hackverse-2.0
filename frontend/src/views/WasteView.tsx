/**
 * Waste view — rework, review latency, CI waste, key-person risk.
 * Owner: Livana (frontend lane).
 * Phase: Tier 1.
 *
 * Consumes backend/app/api/waste.py via ../api/waste.ts.
 */
export default function WasteView() {
  return <div>WasteView — TODO: waste detectors dashboard.</div>;
}
