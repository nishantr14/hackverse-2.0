"""
Placeholder test.
Owner: whoever lands the first real backend logic — replace this with a
real test for that module. Phase: Tier 0 (keeps `pytest` green from commit
one so CI/local runs never fail on "no tests collected").
"""


def test_placeholder():
    assert True
