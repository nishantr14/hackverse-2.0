"""
SQLAlchemy models.
Owner: shared infra (whoever touches this must keep it matching
docs/schema.sql exactly — that file is frozen and requires all four
teammates present to change).
Phase: Tier 0.
"""

# TODO: SQLAlchemy declarative models for actor, work_item, event_log,
# cost_event, rate_card, ci_run, ai_usage, variant — 1:1 with
# docs/schema.sql column-for-column.
